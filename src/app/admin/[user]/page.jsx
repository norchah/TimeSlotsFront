import React from 'react';

export default function UserPage({id}) {
  console.log('user', id);
  return (
    <div>
      {/*<p>{user.id}</p>*/}
      {/*<p>{user.username}</p>*/}
      {/*<p>{props.roles}</p>*/}
    </div>
  );
}